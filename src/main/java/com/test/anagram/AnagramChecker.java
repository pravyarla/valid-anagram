package com.test.anagram;


public class AnagramChecker {

    public static boolean isAnagram(String firstString, String secondString) {

        //return true--> if two Strings have exact same characters, with the same frequency, but arranged in a different order

        //return false -->if two Strings does not have same characters and if letters are same and the frequency doesn't match

        return true;

    }
}
